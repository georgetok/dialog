include ./modal
include ./form

-
  const modalID = 'modal-offer';
  const formIDNumber = 2;
  const isModal = true;
  const formID = 'modal-offer-form'

mixin modal-offer
  +modal(modalID)
    +form(isModal, formIDNumber, formID)
