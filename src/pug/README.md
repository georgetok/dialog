## Папка для файлов шаблонизатора PUG
---
Все страницы создаются на основе компонента layout (global/layout.pug).

Структура:
* Компоненты — pug/components;
* Миксины (каждый в отдельном файле) — pug/mixins;
* Страницы — pug/page.

