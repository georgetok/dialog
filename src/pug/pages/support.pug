extends ../global/layout
include ../mixins/svg
include ../mixins/button
include ../mixins/button-play
include ../mixins/picture
include ../mixins/form-feedback
include ../mixins/page-title

block variables
  -
    const title = t.meta.support_title;
    const desc = t.meta.main_desc;
    const routeClass = "js-route-main";
    const pageName = "support";

block content
  +page-title(t.support.title)
  .feedback
    .wrapper.wrapper--main
      .wrap.wrap--as.wrap--sb.wrap--main
        .col.col--left
          +form-feedback(false, 1, 'form-feedback')
        .col.col--right
          .card.card--support
            .wrap
              img(src='/img/icon-support.svg' alt=t.alt.support)
            h2.card__title=t.support.letter
            p.card__sub=t.support.connect
            a.link--with-icon.link.link--accent.link--icon-accent(
              href="https://support.dlg.im/"
              target="_blank"
            ) support@dlg.im
              +svg('arrow-right',15, 13)